const base = require('./base');
const managerCard = require('./managerCard');
const engineerCard = require('./engineerCard');
const internCard = require('./internCard');

module.exports = {base, managerCard, engineerCard, internCard}